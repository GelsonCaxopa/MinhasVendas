import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('MenuPgPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(MenuPgPage(title: 'MenuPg')));
    //  final titleFinder = find.text('MenuPg');
    //  expect(titleFinder, findsOneWidget);
  });
}
