import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:minhas_vendas/app/menu/menu_module.dart';

void main() {
  initModule(MenuModule());
  // MenuPgController menupg;
  //
  setUp(() {
    //     menupg = MenuPgModule.to.get<MenuPgController>();
  });

  group('MenuPgController Test', () {
    //   test("First Test", () {
    //     expect(menupg, isInstanceOf<MenuPgController>());
    //   });

    //   test("Set Value", () {
    //     expect(menupg.value, equals(0));
    //     menupg.increment();
    //     expect(menupg.value, equals(1));
    //   });
  });
}
